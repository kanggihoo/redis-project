# Observability

이 문서는 기본 템플릿이다. Phase 성격에 따라 섹션을 추가하거나 삭제할 수 있다.

| Area | Metric | Purpose |
|---|---|---|
| Redis | keyspace hits/misses | cache hit ratio |
| Redis | memory used | 캐시 메모리 사용량 |
| DB | query count | DB 부하 감소량 |
| k6 | p95 / p99 | 응답 시간 변화 |
